# Features

Cada capacidade funcional futura deve viver em um módulo próprio neste diretório. Componentes,
schemas e serviços específicos permanecem próximos; acesso a banco e autorização ficam em
`src/server` e nunca dentro de componentes React.
